//describe("TripList.Models.Places", function() {
  //it("should know when it is marked as complete", function() {
    //var item = new TripList.Models.Place({ isComplete: false });
    //expect(item.get('isComplete')).toBe(true);
  //});
//});
